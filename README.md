# chewie-crypto

This crate provides the base for secret and cryptographic operations for chewie-auth. It provides the base
crates, and utilities around the traits; but most implementations will be in separate crates.
